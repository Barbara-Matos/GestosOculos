<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.android.glass.cardsample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme"
        tools:replace="android:label">
        <activity android:name=".click"/>
        <activity android:name=".foto" />
        <activity android:name=".ficheiros" />
        <activity android:name=".historico" />
        <activity android:name=".ordem_trabalho" />
        <activity android:name=".videochamada" />
        <activity android:name=".MaquinaXXX" />
        <activity android:name=".otas" />
        <!--
      Changes Glass touchpad response from the following key strokes:
        - Enter
        - Tab
        - Shift + Tab
        - Back button
        - Home button
        - Arrows
      to the motion events, enabling this app to use the touch gestures.
        -->
        <meta-data
            android:name="com.google.android.glass.TouchEnabledApplication"
            android:value="true" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.google.android.glass.category.DIRECTORY" />
            </intent-filter>
        </activity>
        <activity
            android:name=".menu.MenuActivity"
            android:theme="@style/MenuTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>